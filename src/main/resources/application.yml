spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  profiles:
    default: dev
  liquibase:
    change-log: classpath:/db/changelog/maser.xml
    enabled: false
  security:
    user:
      name: admin
      password: pass
  sql:
    init:
      mode: always


